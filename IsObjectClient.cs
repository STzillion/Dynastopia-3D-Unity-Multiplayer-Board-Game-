using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Unity.Netcode;

public class IsObjectClient : NetworkBehaviour
{
   

    // Update is called once per frame
    void Update()
    {
        if(IsClient)
        {

        }
    }
}
